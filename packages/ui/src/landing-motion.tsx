"use client";

// Share the animation runtime already used by the UI package.
export {
  motion,
  AnimatePresence,
  MotionConfig,
  useReducedMotion,
  useInView,
  useMotionValue,
  useTransform,
  useSpring,
  animate,
} from "motion/react";
export type {
  DOMMotionComponents,
  Variants,
  MotionProps,
  Transition,
  MotionValue,
} from "motion/react";
