name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    services:
      mongodb:
        image: mongo:8
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --quiet --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run check-types
      - run: npm run lint
      - run: npm test -w apps/backend -- --runInBand
      - name: Validate production configuration contract
        env:
          MONGODB_URL: mongodb+srv://ci:ci@mongo.example/gym4me
          REDIS_URL: rediss://ci:ci@redis.example:6380
          JWT_SECRET: ci-production-jwt-secret-with-32-characters
          MOCK_PAYMENT_CALLBACK_SECRET: ci-payment-callback-secret-with-32-characters
          EXPORT_STORAGE_DRIVER: gcs
          EXPORT_GCS_BUCKET: gym4me-ci-exports
          APP_RELEASE: ci-production-contract
          SENTRY_DSN: https://public@example.ingest.sentry.io/1
          FIREBASE_PROJECT_ID: gym4me-ci
          FIREBASE_CLIENT_EMAIL: firebase-admin@example.iam.gserviceaccount.com
          FIREBASE_PRIVATE_KEY: ci-private-key
          KAVENEGAR_API_KEY: ci-kavenegar-key
          CORS_ORIGINS: https://gym4me.example
        run: npm run validate:production -w apps/backend
      - name: Bootstrap backend and verify health checks
        env:
          NODE_ENV: test
          PORT: 7088
          MONGODB_URL: mongodb://127.0.0.1:27017/gym4me-ci
          REDIS_URL: redis://127.0.0.1:6379
          JWT_SECRET: ci-only-jwt-secret-value
          CORS_ORIGINS: http://127.0.0.1:7080
        run: |
          npm run build -w apps/backend
          npm run start:ci -w apps/backend > /tmp/gym4me-backend.log 2>&1 &
          backend_pid=$!
          trap 'kill "$backend_pid" 2>/dev/null || true' EXIT

          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:7088/health/live >/dev/null; then
              break
            fi
            if ! kill -0 "$backend_pid" 2>/dev/null; then
              cat /tmp/gym4me-backend.log
              exit 1
            fi
            sleep 1
          done

          curl --fail --show-error http://127.0.0.1:7088/health/live
          curl --fail --show-error http://127.0.0.1:7088/health/ready
      - run: npm run build

  android-debug:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - run: npm ci
      - run: npm run cap:sync:hosted -w apps/application
      - run: ./gradlew --no-daemon :app:assembleDebug
        working-directory: apps/application/android
      - uses: actions/upload-artifact@v4
        with:
          name: gym4me-debug-apk
          path: apps/application/android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

  application-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
        working-directory: apps/application
      - run: npm run e2e -w apps/application
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: application-playwright-report
          path: |
            apps/application/playwright-report
            apps/application/test-results
          if-no-files-found: ignore
