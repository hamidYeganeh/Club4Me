export type DiscoveryClubsCatalogSectionsProps = {
  /** Prefix for section ids so home/clubs don't collide. */
  idPrefix?: string;
  showClubTypes?: boolean;
  showHero?: boolean;
  /** Full-width editorial club cards after curated rails. */
  showEditorialList?: boolean;
};
