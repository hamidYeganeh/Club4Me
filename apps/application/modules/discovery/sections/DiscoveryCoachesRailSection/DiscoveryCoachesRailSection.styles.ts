import { tv } from "tailwind-variants";

export const discoveryCoachesRailSectionStyles = tv({
  slots: {
    root: "flex flex-col gap-4",
    swiper: "w-full",
    slide: "h-auto! w-auto!",
  },
});
