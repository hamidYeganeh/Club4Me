import { tv } from "tailwind-variants";

export const accountAuthOtpCopySectionStyles = tv({
  slots: {
    root: "flex w-full max-w-sm flex-col items-center px-2 text-center",
    title: "mt-0",
    subtitle: "mt-3 max-w-[22rem]",
  },
});
