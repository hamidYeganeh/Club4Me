import { tv } from "tailwind-variants";

export const accountAuthRolesCopySectionStyles = tv({
  slots: {
    root: "flex w-full max-w-sm flex-col items-start self-stretch pt-3 text-start sm:self-center",
    title: "mt-0",
    subtitle: "mt-2 max-w-[22rem]",
  },
});
