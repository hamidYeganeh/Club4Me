import { tv } from "tailwind-variants";

export const accountAuthHomeHeroSectionStyles = tv({
  slots: {
    root: "pointer-events-none absolute inset-0 overflow-hidden",
    image: "object-cover object-[center_18%]",
  },
});
