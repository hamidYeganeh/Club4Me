import { tv } from "tailwind-variants";

export const accountAuthOtpCopySectionStyles = tv({
  slots: {
    root: "flex w-full max-w-md flex-col items-start text-start",
    title: "mt-0",
    subtitle: "mt-3 max-w-[28rem]",
  },
});
